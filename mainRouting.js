import React from 'react';
import ReactDOM from 'react-dom';
import CustomRouterLink from './app/RouterLink';

ReactDOM.render(
        <CustomRouterLink/>,
        document.getElementById('routeApp')
      );